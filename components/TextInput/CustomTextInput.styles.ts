import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  base: {
    height: 48,
    paddingHorizontal: 16,
    paddingVertical: 12,
    borderRadius: 10,
    borderWidth: 1,
    fontSize: 16,
  },
  error: {
    borderColor: 'red',
  },
});